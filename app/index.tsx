import React from 'react';
import { View } from 'react-native';

export default function Index() {
  // Root output is managed by AppNavigator via app/_layout.tsx.
  return <View style={{ flex: 1 }} />;
}
